# Rough Idea

When scripture is fetched via `JWToolsFetchScripture`, automatically copy the text to a Vim register. Add a `<leader>jp` keybinding to paste that scripture at the current cursor position.
